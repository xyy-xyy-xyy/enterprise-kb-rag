# 安全设计说明（国密安全层）

本文说明 EnterpriseKB 国密安全层的**威胁模型、加密范围、密钥管理与已知限制**。

请先读第 2 节和第 5 节：**加密存储解决的是"磁盘上的文档被拿走"，不解决"向量库里的文本被读到"。**
说清楚边界比堆砌算法更有意义。

---

## 1. 威胁模型

### 1.1 我们防什么

| 威胁 | 防御手段 | 效果 |
|---|---|---|
| 服务器磁盘/备份被拷走，原始 PDF/Word 被直接打开 | 文档以 SM4-CBC 密文（`.enc`）落盘，明文不驻留 | ✅ 无密钥无法还原 |
| 攻击者只拿到密文，想从中分析出内容 | 每次加密使用随机 IV，相同文档两次加密结果不同 | ✅ 无法做密文比对、无法识别"这两份是同一份文档" |
| 文档在存储期间被人悄悄改动（篡改/误覆盖） | 解析前重算 SM3 摘要，与索引中记录的摘要比对（`--verify-all`） | ✅ 能发现，但不能自动还原 |
| 同一份文档换个文件名重复上传，撑大索引 | SM3 **内容**摘要去重（不依赖文件名） | ✅ 换名也拦得住 |
| 密钥被误提交进 Git 仓库 | `.gitignore` 忽略密文、备份与密钥文件；`.env.example` 只放占位符 | ✅ 见 4.4 |

### 1.2 我们不防什么（重要）

- **不防"能访问向量数据库的人"。** 索引里存的是**明文分块文本**（见第 3 节），拿到 Qdrant/FAISS 即拿到知识库内容。
- **不防"能访问运行中服务的人"。** 服务进程内持有密钥与明文，`/api/ask` 本身就是一个合法的"解密读取"接口。有查询权限的人就能读到内容 —— 这是业务需求，不是漏洞。
- **不防内存取证 / root 权限攻击者。** 密钥以明文形式存在于进程内存与环境变量中。
- **不提供抗抵赖/签名能力。** SM3 是摘要，不是 MAC，也不是签名。本方案可检测**意外损坏与明显篡改**，但**不具备密码学意义上的抗伪造能力**：能改写密文的人同样能改写索引里的摘要记录。
- **不做传输加密。** HTTPS 由部署层（反向代理/网关）负责，本服务默认监听 HTTP。

一句话：本层是**静态数据（data-at-rest）保护**，不是端到端安全方案。

---

## 2. 加密范围

### 2.1 加密了什么

| 对象 | 位置 | 形态 |
|---|---|---|
| 知识库原始文档 | `data/docs/*.pdf.enc`、`data/docs/*.docx.enc` | `IV(16 字节) ‖ SM4-CBC(PKCS7 填充后的明文)` |

- 算法：**SM4-CBC**，密钥 16 字节，分组 16 字节，PKCS7 填充。
- **IV 每次加密随机生成**（`os.urandom(16)`），随密文一起存进文件头，解密时读出。
- 实现见 [`app/crypto.py`](app/crypto.py)：`encrypt_sm4` / `decrypt_sm4`。
- 只用 `cryptography`（项目原有依赖），**零新增依赖**，不引入 `gmssl`。

> **为什么必须随机 IV、绝不能用 ECB**：ECB 与固定 IV 下，相同明文块产生相同密文块。
> 攻击者不需要密钥，就能看出两份文档是否相同、文档中哪些段落重复，甚至利用中文文档的
> 固定排版做逐块猜测。更危险的是：**代码照样跑得通、测试照样通过、不报任何错**。
> 因此 `tests/test_crypto.py::test_same_plaintext_differs_each_time` 专门断言
> "同一明文两次加密的 IV 与密文都不同"，把这条约束钉在测试里。

### 2.2 明文如何不落盘

解密后**只在内存里解析**，不写临时文件：

```python
# 正确：密文 → 内存 bytes → 解析器直接读
pymupdf.open(stream=data, filetype="pdf")
docx.Document(io.BytesIO(data))
```

`app/crypto.py` **刻意不提供** `decrypt_to_temp_file` 这类接口。
理由是：明文一旦落盘，即使随后删除，在日志型文件系统、SSD 磨损均衡、备份快照里都可能残留，
"加密存储"会在最后一步前功尽弃。见 `read_document_bytes`（[`app/ingestion.py`](app/ingestion.py)）。

### 2.3 逻辑名与磁盘名的分离

磁盘上是 `外派人员费用管理规定.pdf.enc`，但入库的 `file_name` / `source` / 界面显示一律是
`外派人员费用管理规定.pdf`（`crypto.logical_name` / `logical_source` 负责剥离 `.enc`）。

两个原因：
1. 否则同一份文档的明文版与密文版会被判成两个文档，重复入库；
2. 否则引用溯源会显示 `xxx.pdf.enc`，用户看不懂，演示效果也差。

---

## 3. 为什么索引里的文本不加密

**这是本设计中最需要讲清楚的一个取舍。**

### 3.1 事实

向量索引（FAISS / Qdrant）中每个分块的 payload 里，`page_content` 是**明文**：

```json
{"page_content": "第三章 报销流程 …… 一线城市住宿费上限 500 元 ……",
 "metadata": {"file_name": "外派人员费用管理规定.pdf", "page": 3, "sm3_hash": "…"}}
```

### 3.2 为什么不加密

- **检索必须能看到明文。** 向量检索按语义相似度排序，BM25 按词频排序，Reranker 按原文重排 —— 三者都需要真实文本。若把 payload 加密，就必须在**每次检索时把整个索引解密一遍**，索引从"可检索结构"退化成"一个大密文块"，性能上不可接受。
- **加密了也拦不住谁。** 能连上向量库的人，本来就能调用 `/api/ask` 拿到答案。把 payload 加密只是把攻击成本从"读数据库"抬高到"读数据库 + 偷密钥"，而密钥就在同一台机器的环境变量里 —— 这是**安全剧场**，不是安全。
- **真正该做的是访问控制。** 索引的防护应落在：Qdrant 只监听内网 / 开启 API Key、数据库账号最小权限、磁盘加密。这些属于部署层，不属于应用层。

### 3.3 这个取舍意味着什么

> **如果你需要"索引内容也不可读"，本方案不满足该需求，需要另行设计**（例如按租户分库 + 每租户独立密钥 + 检索时在可信执行环境内解密）。
> 本层的定位是：**让`data/docs/` 下的原始文档在磁盘上是密文** —— 这已经覆盖了"文件被拷走/误传到网盘/备份泄漏"这类最常见的事故。

---

## 4. 密钥管理

### 4.1 密钥来源与优先级

`crypto.load_or_create_key()` 按以下顺序取密钥，**先命中者胜**：

| 优先级 | 来源 | 适用场景 |
|---|---|---|
| 1 | 环境变量 `SM4_KEY`（32 位 hex） | **生产环境**（由密钥管理服务/K8s Secret 注入） |
| 2 | 密钥文件 `SM4_KEY_FILE`（默认 `data/.sm4_key`，内容为 hex 文本） | 单机部署、开发 |
| 3 | 自动生成并写入密钥文件，**同时打 WARNING** | 仅开发/演示 |

第 3 级是为了"克隆仓库后能直接跑起来"而保留的便利，**它不是安全默认值**：

```
WARNING app.crypto: 开发模式：未配置 SM4_KEY，已自动生成开发密钥并写入
D:\...\data\.sm4_key。生产环境请通过环境变量注入，且不要把密钥提交进仓库。
```

日志里**只打印密钥文件路径与长度，绝不打印密钥值**（`_parse_key` 也只告警长度问题，不回显内容）。

### 4.2 生成密钥

```bash
venv\Scripts\python.exe -m app.crypto --gen-key
```

会写入密钥文件并打印 `SM4_KEY=<hex>`，供你复制到 `.env` 或密钥管理服务。
文件权限在类 Unix 系统上会尽量收紧到 `0600`（Windows 下 `chmod` 基本无效，不作为错误）。

### 4.3 密钥丢失的后果

**密钥丢失 = 密文永久无法恢复。** 没有后门、没有找回机制、没有主密钥托管。

- 请对密钥文件做**独立于文档的**备份（和密文放一起备份等于没备份）。
- 轮换密钥需要重新加密全部文档：`--decrypt-all`（用旧密钥）→ 换密钥 → `--encrypt-all`。
  本阶段**未提供**在线轮换工具。

### 4.4 密钥不进 Git

`.gitignore` 已忽略：

```
data/docs/*.enc        # 密文
data/docs/*.bak        # 加密前的明文备份
data/.sm4_key          # 密钥文件
data/**/.sm4_key
*.key
```

仓库里只有 [`.env.example`](.env.example)，里面的 `SM4_KEY` 是**空占位符**。

> **密钥进仓库 = 本阶段白做。** 代码写得再对，密钥一旦推上公开仓库，所有密文都等同于明文。
> 提交前请务必 `git status` 确认没有 `.enc` / `.sm4_key` / 真实 `.env` 混进来。

### 4.5 明文备份（`.bak`）的处理

`--encrypt-all` **不删除**原明文，而是改名为 `.bak` 保留：

```
a.docx  →  a.docx.enc（密文）+ a.docx.bak（原明文）
```

这看起来"留了个明文后门"，但这是**刻意的**：批量加密是不可逆操作，中途断电/密钥写错都可能让文档永久读不出来。
留一份 `.bak` 让人有后悔药。`.bak` 同样被 `.gitignore` 忽略。

**确认系统跑通后，请自行删除 `.bak`**（它们等价于未加密的原文）：

```bash
# 确认无误后再执行；Windows 下用 del
rm data/docs/*.bak
```

---

## 5. 已知限制

1. **索引里的分块文本是明文**（第 3 节）——这是本方案最大的边界，不满足"索引也不可读"的需求。
2. **SM3 不具备抗伪造能力**。摘要与密文存在同一台机器上，能改密文的人也能改摘要记录。它防的是**意外损坏**，不是**有能力的攻击者**。
3. **SM4-CBC 不带认证（无 MAC/AEAD）**。填充校验只能以约 1/256 的概率漏掉随机篡改，且**不防重放、不防块重排**。若需认证加密，应改用 SM4-GCM 或"先 SM3-MAC 再加密"的组合（本阶段未做）。
4. **密钥不轮换、无生命周期管理**，无 HSM/KMS 集成，无审计日志（谁在何时解密了哪份文档 —— 没有记录）。
5. **`--verify-all` 只比对磁盘文件与索引中的摘要**：如果索引里的文档被删除（本阶段不支持文档删除 API），它会报"在索引里找不到该内容"，这是提示而非错误。
6. **首次加密全部文档后必须重建索引吗？** 不需要。加密只改变磁盘存储形态，`sm3_hash` 与分块内容都不变；已入库的文档在 `--encrypt-all` 后无需 `--rebuild`。
7. **性能**：SM4 加解密在解析阶段同步执行，对当前知识库规模（百级文档）影响可忽略；未做流式加密，大文件会一次性读入内存。
8. **不支持加密非 `data/docs/` 下的其他数据**（比如 FAISS 索引文件、日志、`.env` 里的 API Key 本身）。
9. **`ENCRYPT_STORE=false` 会退回明文存储**，此时本层不提供任何保护，仅用于对照演示与故障排查。生产环境请保持 `true`。

---

## 6. 相关命令速查

| 命令 | 作用 |
|---|---|
| `python -m app.crypto --selftest` | SM3 官方向量 + SM4 往返 + IV 随机性自测 |
| `python -m app.crypto --gen-key` | 生成 SM4 密钥并写入密钥文件 |
| `python -m app.crypto --encrypt-all` | `data/docs/` 明文 → `.enc`（明文保留为 `.bak`） |
| `python -m app.crypto --decrypt-all` | `.enc` → 明文（密文保留为 `.bak`） |
| `python -m app.crypto --verify-all` | 解密重算 SM3，与索引比对，列出异常文件 |
| `python -m app.ingestion --encrypt-all` | 同上（转发入口，便于从入库命令直接调用） |
| `python -m app.ingestion --verify-all` | 同上 |
| `python -m pytest tests/test_crypto.py -q` | 国密层单元测试 |
