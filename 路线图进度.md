# 企业知识库 RAG 项目 - 路线图进度

> 对照参考：`rag-project-roadmap.html`
> 当前阶段：**阶段一已完成 → 阶段二进行中**
> 最后更新：2026-09-13

---

## 项目当前状态

| 组件 | 状态 | 说明 |
|---|---|---|
| Python + venv | ✅ | Python 3.13，所有依赖已装 |
| WSL2 | ✅ | 内核 v2.7.14，Ubuntu-22.04 在 D 盘 |
| Docker Desktop | ✅ | v29.7.2，数据在 D 盘 |
| Qdrant | ✅ | v1.19.1，Docker 运行，localhost:6333 |
| 向量后端 | ✅ 双后端 | FAISS + Qdrant，.env 中 VECTOR_BACKEND 切换 |
| RAG 全链路 | ✅ | PDF → 分块 → 向量化 → 检索 → 问答 |
| Web 界面 | ✅ | Gradio 双 Tab（问答 + 入库） |

---

## 阶段一：环境搭建与最小 RAG ✅ 全部完成 8/8

| 任务 | 状态 | 说明 |
|---|---|---|
| 环境搭建（Python + venv + LangChain + FastAPI + PyMuPDF） | ✅ | Python 3.13 |
| 向量数据库部署 | ✅ | FAISS 先跑通 → Qdrant 已切换，Docker 运行 |
| 文档解析模块（PDF） | ✅ | PyMuPDFLoader |
| 文档分块 | ✅ | RecursiveCharacterTextSplitter，chunk_size=500, overlap=50 |
| Embedding 接入 | ✅ | DashScope text-embedding-v2 |
| 向量入库 | ✅ | FAISS + Qdrant 双后端，VECTOR_BACKEND 切换 |
| 检索 + 生成回答 | ✅ | similarity_search_with_score + ChatTongyi + prompt |
| Gradio 演示界面 | ✅ | 双 Tab：知识问答 + 文档入库 |

---

## 阶段二：核心功能完善 ⏳ 进行中

### 2.1 流式输出（推荐第一个做）

**目标**：LLM 回答逐字流式返回，用户不用等完整回答才看到内容。

**要改的文件和具体做法**：

#### `app/retrieval.py`
- 新增 `stream_answer(question, k=None)` 方法
- 用 `ChatTongyi(streaming=True)` 初始化一个支持流式的 LLM 实例
- 调用 `.stream()` 而非 `.invoke()`，返回一个 generator
- generator 每次 yield 一个 dict：`{"type": "chunk", "content": "片段文本"}`
- 结束时 yield `{"type": "sources", "sources": [...]}` 把来源信息带上
- 检索部分不变，还是 `vector_store.search()` 先拿到上下文
- **注意**：`get_llm()` 需要加一个 `streaming=True` 参数，或者单独搞一个 `get_streaming_llm()`

```python
# retrieval.py 新增方法伪代码
def stream_answer(question: str, k: int = None):
    """流式问答，yield {"type": "chunk"|"sources", "content"|"sources": ...}"""
    config.setup_logging()
    k = k or config.TOP_K
    results = vector_store.search(question, k=k)
    if not results:
        yield {"type": "chunk", "content": NO_RESULT_ANSWER}
        yield {"type": "sources", "sources": []}
        return
    context = _build_context(results)
    prompt = PROMPT_TEMPLATE.format(context=context, question=question)
    for chunk in get_streaming_llm().stream([HumanMessage(content=prompt)]):
        yield {"type": "chunk", "content": chunk.content}
    yield {"type": "sources", "sources": _to_sources(results)}
```

#### `app/main.py`
- 新增 SSE 端点 `GET /api/ask/stream`，用 `StreamingResponse` + `text/event-stream`
- Gradio 界面的"提问"按钮改为流式输出，用 `gr.ChatInterface` 或手动 yield
- **注意**：Gradio 的流式用 `yield` 实现，函数声明为 generator

```python
# main.py 新增 SSE 端点
from fastapi.responses import StreamingResponse
import json

@app.post("/api/ask/stream")
def api_ask_stream(payload: AskRequest):
    def event_stream():
        for item in retrieval.stream_answer(payload.question, k=payload.k):
            yield f"data: {json.dumps(item, ensure_ascii=False)}\n\n"
    return StreamingResponse(event_stream(), media_type="text/event-stream")
```

#### 验收标准
- `POST /api/ask/stream` 返回 SSE 流，逐块推送 answer 内容
- Gradio 界面提问后文字逐字出现，不是一整段蹦出来
- 最后显示参考来源
- 原有的 `/api/ask` 非流式接口保留不动

---

### 2.2 BM25 混合检索

**目标**：向量检索 + 关键词检索（BM25）融合，提升召回率。这是 RAG 核心进阶，面试高频考点。

**要改的文件和具体做法**：

#### 安装依赖
```bash
pip install rank-bm25
```

#### `app/vector_store.py`
- 新增 `hybrid_search(query, k=5)` 函数
- 向量检索：调现有的 `search()` 拿 top-k*2 个候选（多召回一些给融合用）
- BM25 检索：从索引中取出所有文档文本，构建 `BM25Okapi`，查询拿 top-k*2 个候选
- 融合方法：**RRF（Reciprocal Rank Fusion）**
  - 对每个文档，计算 `score = 1/(rank_in_vector + 60) + 1/(rank_in_bm25 + 60)`
  - 按融合分数排序，取 top-k
- 返回格式和 `search()` 一致：`[(Document, score)]`

```python
# vector_store.py 新增
def hybrid_search(query: str, k: int = 5) -> list[tuple[Document, float]]:
    """混合检索：向量 + BM25，RRF 融合。"""
    from rank_bm25 import BM25Okapi

    # 1. 向量检索（多召回）
    vector_results = search(query, k=k * 3)

    # 2. 取出所有文档做 BM25
    store = load_index()
    # FAISS: 从 docstore 取; Qdrant: scroll 取
    all_docs = _get_all_documents(store)
    corpus = [doc.page_content.split() for doc in all_docs]
    bm25 = BM25Okapi(corpus)
    query_tokens = query.split()
    bm25_scores = bm25.get_scores(query_tokens)

    # 3. BM25 top-k*3
    bm25_ranked = sorted(enumerate(bm25_scores), key=lambda x: x[1], reverse=True)[:k*3]

    # 4. RRF 融合
    rrf_k = 60  # 常数
    rrf_scores = {}
    for rank, (doc, _) in enumerate(vector_results):
        source = doc.metadata.get("source", "") + str(doc.metadata.get("page", ""))
        rrf_scores[source] = rrf_scores.get(source, 0) + 1 / (rank + rrf_k)
    for rank, (idx, _) in enumerate(bm25_ranked):
        doc = all_docs[idx]
        source = doc.metadata.get("source", "") + str(doc.metadata.get("page", ""))
        rrf_scores[source] = rrf_scores.get(source, 0) + 1 / (rank + rrf_k)

    # 5. 排序取 top-k
    sorted_sources = sorted(rrf_scores.items(), key=lambda x: x[1], reverse=True)[:k]
    # 映射回 Document
    ...
```

#### `app/retrieval.py`
- `answer_question()` 里的 `vector_store.search()` 改为 `vector_store.hybrid_search()`
- 新增 `.env` 配置 `HYBRID_SEARCH=true/false` 控制是否启用

#### `app/config.py`
- 新增 `HYBRID_SEARCH = os.getenv("HYBRID_SEARCH", "false").lower() == "true"`

#### 验收标准
- `.env` 设 `HYBRID_SEARCH=true` 后，问答走混合检索
- 对同一问题，混合检索的结果数量 >= 纯向量检索（召回更广）
- 简历能讲：什么是 RRF、为什么融合、BM25 和向量各自的优势

---

### 2.3 BGE Reranker 重排序

**目标**：在检索结果上再上一层交叉编码器重排序，提升精度。

**要改的文件和具体做法**：

#### 安装依赖
```bash
pip install sentence-transformers
# 模型：BAAI/bge-reranker-v2-m3（首次运行自动下载，约 2GB）
```

#### `app/retrieval.py`
- 新增 `_rerank(query, results, top_k=None)` 方法
- 用 `CrossEncoder` 对 (query, doc.page_content) 打分
- 按分数排序，取 top_k

```python
# retrieval.py 新增
from sentence_transformers import CrossEncoder

_reranker = None

def get_reranker():
    global _reranker
    if _reranker is None:
        _reranker = CrossEncoder("BAAI/bge-reranker-v2-m3")
    return _reranker

def _rerank(query, results, top_k=None):
    pairs = [(query, doc.page_content) for doc, _ in results]
    scores = get_reranker().predict(pairs)
    reranked = sorted(zip(results, scores), key=lambda x: x[1], reverse=True)
    if top_k:
        reranked = reranked[:top_k]
    return [(doc, score) for (doc, _), score in reranked]
```

#### `app/config.py`
- 新增 `RERANK_ENABLED = os.getenv("RERANK_ENABLED", "false").lower() == "true"`
- 新增 `RERANK_MODEL = os.getenv("RERANK_MODEL", "BAAI/bge-reranker-v2-m3")`

#### `app/retrieval.py` 的 `answer_question()` 改造
- 检索后如果 `config.RERANK_ENABLED`，调 `_rerank()` 重排序
- 检索时 k 放大（比如 k*3），rerank 后缩回 k

#### 验收标准
- `.env` 设 `RERANK_ENABLED=true` 后，检索结果经过 reranker 重排序
- 对比纯向量检索，rerank 后第一个结果更相关（人工判断）
- 简历能讲：为什么 rerank 提升精度、CrossEncoder vs BiEncoder 区别

---

### 2.4 多轮对话上下文

**目标**：支持多轮对话，LLM 能理解"它"指的是上文提到的实体。

#### `app/retrieval.py`
- 新增 `chat_history` 参数到 `answer_question()`
- 把历史对话拼进 prompt：`{history}\n\n参考资料：...\n\n用户问题：{question}`
- 用 LangChain 的 `ConversationBufferMemory` 或手动拼

#### `app/main.py`
- Gradio 界面改为 `gr.ChatInterface`，自带多轮对话支持
- API 端点加 `history` 参数

#### 验收标准
- 第一问"这个比赛是什么"，第二问"参赛有什么要求"，系统能理解"参赛"指上一轮提到的比赛

---

### 2.5 查询改写（Query Rewrite）

**目标**：用户问题模糊时，用 LLM 改写成更适合检索的 query。

#### `app/retrieval.py`
- 新增 `_rewrite_query(question)` 方法
- 调 LLM 把口语问题改成关键词查询
- 示例："那个比赛怎么报名" → "AI先锋未来人才大赛 报名流程"

#### `app/config.py`
- 新增 `QUERY_REWRITE = os.getenv("QUERY_REWRITE", "false").lower() == "true"`

---

### 2.6 多种分块策略

**目标**：支持语义分块（Semantic Chunking）和按页分块，对比效果。

#### `app/ingestion.py`
- 新增 `--strategy` 参数：`recursive`（默认）/ `semantic` / `page`
- `semantic`：用 `SemanticChunker` 按语义边界分块
- `page`：每页一个 chunk

---

### 2.7 引用溯源展示优化

**目标**：Gradio 界面上点击来源可展开原文片段。

#### `app/main.py`
- 来源列表改为可折叠的 Accordion 组件
- 每个来源显示文件名、页码、匹配分数、原文片段

---

### 2.8 文档管理功能

**目标**：查看已入库文档列表，删除单个文档。

#### `app/main.py`
- 新增 `GET /api/documents` 列出已入库文档
- 新增 `DELETE /api/documents/{filename}` 删除文档 + 重建索引
- Gradio 新增"文档管理"Tab

---

## 阶段三：国密安全层 🔒 差异化亮点

### 3.1 国密库集成

**目标**：集成 gmssl-python，支持 SM2/SM3/SM4 算法。

```bash
pip install gmssl
```

#### `app/crypto.py`（新文件）
- `encrypt_sm4(plaintext, key)` → 返回密文
- `decrypt_sm4(ciphertext, key)` → 返回明文
- `hash_sm3(data)` → 返回哈希
- `generate_key()` → 生成随机 SM4 密钥

### 3.2 文档加密存储

**目标**：上传的 PDF 文档加密后存储在 `data/docs/`，解密在内存中进行。

#### `app/ingestion.py`
- `_split_pdf()` 改为先解密再解析
- 加密后的文件用 `.enc` 后缀
- 密钥从 `.env` 的 `SM4_KEY` 读取

### 3.3 向量数据加密方案

**目标**：设计并文档化"为什么向量不加密"的方案。

- 向量本身是浮点数组，不包含可读信息
- Qdrant 支持 payload 加密（metadata 加密，向量不加密）
- 写安全设计文档说明取舍

### 3.4 SM3 完整性校验

**目标**：每个文档入库时算 SM3 哈希，存到 metadata，检索时校验。

#### `app/ingestion.py`
- `_split_pdf()` 里加 `metadata["content_hash"] = sm3(file_bytes)`
- `answer_question()` 返回结果带 `content_hash` 供前端校验

### 3.5 密钥管理方案

**目标**：密钥不硬编码，从环境变量或密钥文件读取。

- `.env` 中 `SM4_KEY=xxx`（开发阶段）
- 生产阶段用环境变量注入或密钥文件
- 文档说明密钥轮换流程

### 3.6 安全设计文档 SECURITY.md

**目标**：GitHub 项目根目录放 SECURITY.md，说明安全设计。

内容模板：
- 威胁模型：保护什么、防什么
- 加密方案：SM4 文档加密 + SM3 完整性校验
- 密钥管理：密钥从哪来、怎么轮换
- 向量不加密的理由
- 安全限制和已知风险

---

## 阶段四：RAG 质量评测体系 📊

### 4.1 评测数据集构建

**目标**：基于知识库文档，手工构建 50 个 QA 对。

#### `data/eval/qa_pairs.json`
```json
[
  {
    "question": "这个比赛的全称是什么",
    "expected_answer": "AI先锋未来人才大赛「AI for Future Talent」",
    "expected_source": "飞行社学习指南.pdf",
    "expected_page": 1,
    "tags": ["事实型"]
  }
]
```
- 事实型问题 20 个（谁/什么/什么时候）
- 推理型问题 20 个（为什么/怎么做）
- 对比型问题 10 个（A 和 B 的区别）

### 4.2 检索准确率评测

**目标**：计算 Hit Rate 和 MRR。

#### `app/eval/retrieval_eval.py`（新文件）
```python
def evaluate_retrieval(qa_pairs, search_func, k=5):
    """返回 {"hit_rate": float, "mrr": float}"""
    hits, mrr_sum = 0, 0
    for qa in qa_pairs:
        results = search_func(qa["question"], k=k)
        sources = [r[0].metadata.get("file_name") for r in results]
        if qa["expected_source"] in sources:
            hits += 1
            rank = sources.index(qa["expected_source"]) + 1
            mrr_sum += 1 / rank
    return {"hit_rate": hits/len(qa_pairs), "mrr": mrr_sum/len(qa_pairs)}
```

### 4.3 回答质量评测

**目标**：用 LLM-as-judge 对回答打分。

#### `app/eval/answer_eval.py`（新文件）
- 用 ChatTongyi 对 (question, answer, expected_answer) 打分
- 维度：准确性（0-5）、相关性（0-5）、完整性（0-5）

### 4.4 对比实验

**目标**：4 种方案对比，生成表格。

| 方案 | Hit Rate | MRR | 回答准确率 |
|---|---|---|---|
| 纯向量检索 | ? | ? | ? |
| 纯 BM25 | ? | ? | ? |
| 混合检索 | ? | ? | ? |
| 混合 + Reranker | ? | ? | ? |

#### `app/eval/run_eval.py`（新文件）
- 依次跑 4 种方案，结果写入 `data/eval/results.json`
- 生成 Markdown 对比表格

### 4.5 评测报告生成

**目标**：自动生成 `data/eval/report.md`，含表格和结论。

### 4.6 人工评测闭环

**目标**：Gradio 加一个"标注"Tab，人工对回答打分，反馈到数据集。

---

## 阶段五：工程化打磨与简历包装 🚀

### 5.1 Git 初始化 + 推 GitHub

```bash
cd D:\new-project\enterprise-kb-rag
git init
git add .
git commit -m "feat: enterprise KB RAG with FAISS/Qdrant dual backend"
# 在 GitHub 创建仓库后
git remote add origin https://github.com/你的用户名/enterprise-kb-rag.git
git push -u origin main
```

**注意**：`.gitignore` 要确保排除 `.env`、`venv/`、`data/faiss_index/`、`data/docs/*.pdf`

### 5.2 README.md

**目标**：GitHub 门面，面试官第一眼看的。

内容结构：
- 项目标题 + 一句话描述
- 架构图（用文字画或 mermaid）
- 技术栈表格
- 快速开始（clone + install + run）
- 功能截图
- 评测结果表格
- 项目路线图

### 5.3 单元测试

**目标**：pytest 覆盖率 >= 70%。

#### `tests/` 目录
```
tests/
├── test_config.py       # 配置加载测试
├── test_vector_store.py # 索引创建/检索测试
├── test_ingestion.py    # PDF 解析/分块测试
├── test_retrieval.py    # 问答/流式测试
└── test_crypto.py       # 加密/解密测试（阶段三后）
```

### 5.4 GitHub Actions CI/CD

#### `.github/workflows/ci.yml`
- trigger: push to main, pull request
- 步骤：checkout → setup python → pip install → pytest → mypy（可选）
- badge 放到 README.md

### 5.5 Docker 部署

#### `Dockerfile`
```dockerfile
FROM python:3.13-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### `docker-compose.yml`
- 服务 1：qdrant
- 服务 2：app（依赖 qdrant）
- volumes 挂到 D 盘

### 5.6 架构图 + 演示截图

- 用 mermaid 画架构图，放到 README.md
- 截 3 张图：Gradio 问答界面、文档入库、Qdrant Dashboard

---

## 给 WorkBuddy / Claude Code 的执行说明

### 当前代码结构

```
enterprise-kb-rag/
├── app/
│   ├── __init__.py
│   ├── config.py          # 配置中心，所有 env 读取
│   ├── vector_store.py    # 向量索引（FAISS + Qdrant 双后端）
│   ├── ingestion.py       # PDF → 分块 → 向量化 → 入库
│   ├── retrieval.py       # 检索 → prompt → LLM → 答案
│   └── main.py            # FastAPI 路由 + Gradio 界面
├── data/
│   ├── docs/              # 知识库 PDF 文档
│   └── faiss_index/       # FAISS 索引（Qdrant 模式下不用）
├── venv/
├── .env                   # 配置文件（不入 Git）
├── .gitignore
├── requirements.txt
├── 启动服务.bat
└── 路线图进度.md           # 本文件
```

### 已验证的导入路径

```python
# 全部实测通过，不会有 ModuleNotFoundError
from langchain_community.embeddings import DashScopeEmbeddings
from langchain_community.chat_models import ChatTongyi
from langchain_community.document_loaders import PyMuPDFLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_community.vectorstores import FAISS
from langchain_qdrant import QdrantVectorStore  # 不是 langchain_community.vectorstores.Qdrant
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams
```

### 已验证的 API 参数

- `ChatTongyi(model_name=...)` — 用 `model_name` 不是 `model`
- `QdrantVectorStore(client=..., collection_name=..., embedding=...)` — 用 `embedding` 不是 `embeddings`
- `QdrantVectorStore.from_documents()` — 不能传 `client=` 参数，会内部自己创建
- `FAISS.load_local(..., allow_dangerous_deserialization=True)` — 本地自建索引需要这个参数

### .env 配置项速查

```ini
DASHSCOPE_API_KEY=sk-xxx
FAISS_INDEX_PATH=data/faiss_index
LLM_MODEL=qwen-plus
CHUNK_SIZE=500
CHUNK_OVERLAP=50

VECTOR_BACKEND=qdrant          # faiss 或 qdrant
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION=enterprise_kb

# 阶段二新增（按需开启）
HYBRID_SEARCH=false             # true 开启混合检索
RERANK_ENABLED=false            # true 开启重排序
RERANK_MODEL=BAAI/bge-reranker-v2-m3
QUERY_REWRITE=false             # true 开启查询改写

# 阶段三新增
SM4_KEY=                        # 16字节 SM4 密钥
```

### 执行顺序建议

1. **阶段二 2.1 流式输出** → 改 retrieval.py + main.py
2. **阶段二 2.2 BM25 混合检索** → 改 vector_store.py + retrieval.py + config.py
3. **阶段二 2.3 BGE Reranker** → 改 retrieval.py + config.py
4. **阶段五 5.1 Git 初始化 + README** → 越早推 GitHub 越好
5. **阶段二 2.4 多轮对话** → 改 retrieval.py + main.py
6. **阶段三 国密安全层** → 新建 crypto.py + 改 ingestion.py
7. **阶段四 评测体系** → 新建 eval/ 目录
8. **阶段五 测试 + CI + Docker** → 最后打磨

每完成一个任务，把本文件对应行从 ⏳ 改成 ✅。

---

## 常用命令速查

```bash
# 启动服务（双击 启动服务.bat 也可以）
venv\Scripts\python.exe -m uvicorn app.main:app --port 8000 --reload

# 扫描 data/docs/ 建索引（首次或新增文档后）
venv\Scripts\python.exe -m app.ingestion

# 清空重建索引（换了文档后）
venv\Scripts\python.exe -m app.ingestion --rebuild

# 健康检查
curl http://localhost:8000/api/health

# 提问测试（非流式）
curl -X POST http://localhost:8000/api/ask -H "Content-Type: application/json" -d "{\"question\":\"你的问题\"}"

# 提问测试（流式，阶段二完成后）
curl -X POST http://localhost:8000/api/ask/stream -H "Content-Type: application/json" -d "{\"question\":\"你的问题\"}"

# 启动 Qdrant（如果 Docker 重启后需要）
docker start qdrant

# Qdrant 控制台
# 浏览器打开 http://localhost:6333/dashboard
```
